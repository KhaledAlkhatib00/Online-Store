@model TUITY_STORE.Models.HomeImage

@{
    Layout = "_DashbordLayout";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Details</title>
</head>
<body>
    <div style="margin-left:50px;">
        <h1>Details</h1>

        <div>
            <h4>HomeImage</h4>
            <hr />
            <dl class="row">
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.name)
                </dd>

                <dd class="col-sm-10">
                    <img src=@Url.Content("~/Image/"+Model.ImagePath) alt="Alterenate Text" width="100" height="100" />
                </dd>
            </dl>
        </div>
        <div>
            <a asp-action="Edit" asp-route-id="@Model.Id">Edit</a> |
            <a asp-action="Index">Back to List</a>
        </div>
        </div>
</body>
</html>
